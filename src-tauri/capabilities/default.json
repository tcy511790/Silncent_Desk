{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Allow the main window to use core APIs and listen to Tauri events.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default"
  ]
}
